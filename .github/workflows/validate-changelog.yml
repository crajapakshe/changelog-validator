name: Validate Changelog

on:
  pull_request:
    branches:
      - main

jobs:
  validate:
    name: Validate Changelog
    runs-on: ubuntu-latest
    
    steps:
      # - uses: actions/checkout@v4
      #   with:
      #     fetch-depth: 0  # This ensures we can compare with the base branch
          
      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #     cache: 'npm'
          
      # - name: Install dependencies
      #   run: npm ci
          
      # - name: Validate Changelog
      #   uses: ./
      #   with:
      #     changelog-path: 'CHANGELOG.md'
      #     check-updated: 'true'

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Validate Changelog
        uses: ./
        with:
          changelog-path: 'CHANGELOG.md'
          check-updated: 'true'